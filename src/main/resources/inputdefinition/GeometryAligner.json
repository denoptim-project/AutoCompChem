[
  {
    "key": "REFERENCE",
    "casedKey": "reference",
    "type": "<String>",
    "doc": "The pathname to the file with the reference chemical system. Only SDF files with ONE molecule are acceptable."
  },
  {
    "key": "ATOMIDMAP",
    "casedKey": "atomIdMap",
    "type": "<int>,<int>,<int>... <int>,<int>,<int>...",
    "doc": "When aligning atom sets, this parameter gives the mapping of atom indexes to be used for the alignment. First, the comma-separated list of the atom indexes in the reference, then one or more spaces, and finally the comma-separated list of indexes in the input. Ignored when aligning based on isomorphism."
  },
  {
    "key": "WEIGHTS",
    "casedKey": "weights",
    "type": "<String>,<String>,<String>...",
    "doc": "When aligning atom sets, this parameter gives the weights of each atom mapping used for the alignment. Each weight can be either a double value or an expression. If the given string is parsable to a double, then it is used as the value. Otherwise, the string is interpreted as an expression defining the value (e.g., '${sin(0.2)}' would be evaluated to the value of the sine of 0.2)."
  },
  {
    "embeddedWorker": "autocompchem.molecule.AtomContainerInputProcessor",
    "tag": "the input/output of chemical objects (e.g., molecular geometries)",
    "ignorableItems": [
      "multiGeomMode",
      "inputGeometries"
    ]
  }
]
