#!/bin/bash

function not_passed() {
  echo "NOT Passed: check condition leading to line '$1' of '$0'"
  exit -1
}

echo Skipped: reactivate once done with new woerkers
exit 0

if [ ! -f "cli9.log" ] ; then not_passed $LINENO ; fi

if ! grep -q -i "'wrong_task_identifier' does not match" "cli9.log" ; then not_passed $LINENO ; fi

if ! grep -q 'Termination status: 0' "cli9.log" ; then echo Passed ; exit 0 ; fi

not_passed $LINENO
exit -1
