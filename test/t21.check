#!/bin/bash

grep -q "MoleculeComparator requires SDF files with only one structu" t21.log
if [ $? != 0 ] ; then
echo NOT Passed
exit -1
fi

grep -q 'Termination status: -1' t21.log
if [ $? == 0 ] ; then 
echo Passed
exit 0
else
echo NOT Passed
exit -1
fi
